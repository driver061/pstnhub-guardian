# Copy to the repo root as guardian.env, then: chmod 600 guardian.env
#
# The ONLY secret. Everything else is in guardian.toml.
# Empty or absent = notifications off; the gate itself works exactly the same.
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/XXXX/YYYY
