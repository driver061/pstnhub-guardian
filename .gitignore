# The webhook secret.
*.env
!deploy/example.env

# Per-host config: real log paths and ports. Start from deploy/example.toml.
guardian.toml

# Runtime allow-list state and the built binary.
state/
/pstnhub-guardian
